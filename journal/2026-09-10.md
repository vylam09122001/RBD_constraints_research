---
title: "Project & Repo Setup"
date: 2026-09-10
---

## What I did

1. Set up the local project folder (`RBD_constraints_research`) with the working folder layout
- `/hip` for scenes
- `/otls` for HDAs (kept in expanded/directory format so they diff cleanly in git instead of `/hda`, Houdini's own default)
- `/python` for custom modules
- `/wiki` for constraint/material knowledge base in markdown format
- Houdini's own default project folders (`geo`, `sim`, `abc`, `tex`, `render`, `flip`, `scripts`, `comp`, `audio`, `video`, `desk`).

2. Created the Houdini Project itself (sets `$JOB`), added a `.gitignore` and `.gitattributes` (so Houdini's binary `.hip`/`.hipnc` scene files aren't text-diffed), initialized git, and made the first commit with the scaffold plus a starting blank scene (`hip/rbd_constraints_rs_v001.hipnc`).

3. Created the GitHub repo (`vylam09122001/RBD_constraints_research`) and pushed. Connected it in GitHub Desktop for day-to-day commits going forward.

4. Create `journal` folder to blog daily progress

## Blockers / open questions

N/A. I am researching:
- Behavior of each constraint types
- The logic of constraint types switching in SOP level

## Next up
- Start wiki page for frequently used constraints types in RBD (Glue/Spring/Soft)
- Build the base testbed scene: Voronoi fracture → constraint geometry → Bullet DOP network with the three relationship nodes (Glue/Spring/Soft)
